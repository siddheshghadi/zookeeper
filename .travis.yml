language: java
sudo: false

matrix:
  include:
      os: linux-ppc64le
      jdk: openjdk8

cache:
  directories:
  - "$HOME/.m2"

script: 
  - mvn clean install -DskipTests spotbugs:check
  - mvn test

branches:
  only:
  - branch-3.5.5
